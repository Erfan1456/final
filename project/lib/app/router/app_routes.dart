/// Centralized route constants for the Flutter client.
///
/// Only routes for implemented screens belong here. Product routes such as
/// login, signup, customer, cleaner, admin, booking, and profile are not
/// defined until those features exist.
abstract final class AppRoutes {
  static const String foundationPath = '/';
  static const String foundationName = 'foundation';
}
